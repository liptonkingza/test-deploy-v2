# คู่มือแก้ปัญหา Encoding Error - แบบทีละขั้นตอน

## ⚠️ ปัญหาที่พบ
```
'charmap' codec can't decode byte 0x84 in position 592: character maps to <undefined>
```

## 🎯 วิธีแก้ไข (เลือก 1 วิธี)

---

## วิธีที่ 1: เปลี่ยน Encoding ใน MySQL Workbench (เร็วที่สุด)

### ⚠️ สิ่งสำคัญ: ต้องเปลี่ยน Encoding **ก่อน** คลิก Next!

**ขั้นตอน:**

1. **เมื่อเห็นหน้าต่าง "Table Data Import"** ที่ขั้นตอน "Configure Import Settings"
   - คุณจะเห็น "Encoding: utf-8" อยู่

2. **คลิกที่ dropdown "Encoding"** (ไม่ใช่คลิก Next!)
   - จะเห็นรายการ encoding ต่างๆ

3. **เลือก encoding ใหม่:**
   - ถ้าไฟล์มาจาก Windows/XAMPP: เลือก **`Windows-1252`** หรือ **`cp1252`**
   - ถ้ามีภาษาไทย: เลือก **`TIS-620`** หรือ **`Windows-874`**
   - ถ้าไม่แน่ใจ: ลอง **`ISO-8859-1`** หรือ **`Latin-1`**

4. **รอสักครู่** - MySQL Workbench จะพยายามอ่านไฟล์ใหม่

5. **ตรวจสอบว่า "Columns:" แสดงคอลัมน์แล้ว** (ไม่ว่างเปล่า)
   - ถ้ายังว่างเปล่า = encoding ยังไม่ถูกต้อง ลอง encoding อื่น

6. **เมื่อเห็นคอลัมน์แล้ว** ค่อยคลิก **"Next >"**

### ❌ สิ่งที่ห้ามทำ:
- ❌ อย่าคลิก "Next >" ก่อนเปลี่ยน encoding
- ❌ อย่าปิด error dialog แล้วคลิก Next ทันที (ต้องเปลี่ยน encoding ก่อน)

---

## วิธีที่ 2: แปลงไฟล์ CSV เป็น UTF-8 (แนะนำถ้าวิธีที่ 1 ไม่ได้)

### ใช้ Notepad++ (ฟรี, แนะนำ)

1. **ดาวน์โหลด Notepad++** (ถ้ายังไม่มี)
   - https://notepad-plus-plus.org/downloads/

2. **เปิดไฟล์ CSV ด้วย Notepad++**
   - ไฟล์ CSV ที่คุณต้องการ import

3. **ดู encoding ปัจจุบัน**
   - ดูที่มุมขวาล่างของ Notepad++
   - จะเห็น เช่น `ANSI`, `Windows-1252`, `TIS-620` เป็นต้น

4. **ตั้งค่า encoding ให้ถูกต้องก่อน** (ถ้ายังไม่ถูกต้อง)
   - ไปที่เมนู **Encoding** > **Character sets**
   - ถ้าเป็นภาษาไทย: **Thai > TIS-620**
   - ถ้าเป็น Windows: **Western European > Windows-1252**
   - ตรวจสอบว่าข้อมูลแสดงผลถูกต้อง (ไม่มีตัวอักษรแปลกๆ)

5. **แปลงเป็น UTF-8**
   - ไปที่เมนู **Encoding** > **Convert to UTF-8**
   - ไฟล์จะถูกแปลงทันที

6. **บันทึกไฟล์**
   - กด **Ctrl+S** หรือ **File > Save**
   - หรือ **File > Save As** เพื่อบันทึกเป็นไฟล์ใหม่

7. **Import ใหม่ใน MySQL Workbench**
   - ใช้ไฟล์ที่แปลงแล้ว
   - ตั้งค่า Encoding เป็น **`utf-8`**

### ใช้ Excel (ถ้ามี)

1. **เปิดไฟล์ CSV ด้วย Excel**
   - ไฟล์ CSV ที่คุณต้องการ import

2. **บันทึกเป็น UTF-8**
   - ไปที่ **File > Save As**
   - เลือก **CSV UTF-8 (Comma delimited) (*.csv)**
   - บันทึกไฟล์

3. **Import ใหม่ใน MySQL Workbench**
   - ใช้ไฟล์ที่บันทึกใหม่
   - ตั้งค่า Encoding เป็น **`utf-8`**

---

## วิธีที่ 3: ใช้ Python Script (สำหรับผู้ใช้ขั้นสูง)

### ติดตั้ง Python (ถ้ายังไม่มี)
- ดาวน์โหลดจาก https://www.python.org/downloads/

### ติดตั้ง chardet (สำหรับตรวจสอบ encoding)
```bash
pip install chardet
```

### ใช้ Script ที่มีอยู่แล้ว
```bash
# แปลงไฟล์ (จะตรวจสอบ encoding อัตโนมัติ)
python convert_csv_encoding.py your_file.csv

# หรือระบุ encoding เอง
python convert_csv_encoding.py your_file.csv --encoding cp1252
python convert_csv_encoding.py your_file.csv --encoding tis-620
```

---

## วิธีที่ 4: Export เป็น SQL แทน CSV (แนะนำที่สุด!)

วิธีนี้จะไม่มีปัญหา encoding เลย เพราะ SQL script ใช้ UTF-8 อยู่แล้ว

### จาก XAMPP (phpMyAdmin)

1. **เปิด phpMyAdmin** (http://localhost/phpmyadmin)

2. **เลือก database** ที่มีข้อมูล

3. **เลือกตาราง** `legacy_customers` (หรือตารางอื่นที่ต้องการ)

4. **คลิกแท็บ "Export"**

5. **ตั้งค่า:**
   - **Format:** เลือก **SQL** (ไม่ใช่ CSV!)
   - **Character set:** เลือก **utf8** หรือ **utf8mb4**
   - ✅ **Add DROP TABLE** (ถ้าต้องการลบตารางเก่าก่อน)
   - ✅ **Add CREATE TABLE** (ถ้าต้องการสร้างตารางด้วย)

6. **คลิก "Go"** เพื่อ export

7. **Import ไปยัง Railway:**
   - เปิด MySQL Workbench เชื่อมต่อกับ Railway
   - เปิดไฟล์ SQL ที่ export มา
   - รัน SQL script (กด Ctrl+Shift+Enter หรือคลิก Execute)

### จาก MySQL Workbench (XAMPP)

1. **เชื่อมต่อกับ XAMPP database**

2. **คลิกขวาที่ตาราง** `legacy_customers`

3. **เลือก "Table Data Export Wizard"**

4. **เลือก "Export to SQL INSERT statements"** แทน CSV

5. **Export และ import ไปยัง Railway**

---

## 🔍 วิธีตรวจสอบ Encoding ของไฟล์

### Windows PowerShell
```powershell
# ตรวจสอบ encoding
Get-Content "file.csv" -Encoding Byte | Select-Object -First 10
```

### Python (ใช้ chardet)
```python
import chardet

with open('file.csv', 'rb') as f:
    raw_data = f.read(10000)
    result = chardet.detect(raw_data)
    print(f"Encoding: {result['encoding']}")
    print(f"Confidence: {result['confidence']:.1%}")
```

---

## 📝 หมายเหตุ

- ไฟล์ CSV จาก XAMPP/Windows มักใช้ encoding **Windows-1252** หรือ **TIS-620** (ถ้ามีภาษาไทย)
- MySQL Workbench มักตั้งค่า default เป็น **UTF-8**
- **วิธีที่ดีที่สุด:** Export เป็น SQL แทน CSV เพื่อหลีกเลี่ยงปัญหา encoding

---

## 🆘 ถ้ายังไม่ได้ผล

1. **ลอง encoding หลายๆ แบบ:**
   - Windows-1252
   - TIS-620
   - Windows-874
   - ISO-8859-1
   - cp1252

2. **ตรวจสอบไฟล์ CSV:**
   - เปิดด้วย Notepad++ ดูว่ามีตัวอักษรแปลกๆ หรือไม่
   - ตรวจสอบว่ามี BOM (Byte Order Mark) หรือไม่

3. **ใช้วิธี Export เป็น SQL แทน** (แนะนำที่สุด!)

