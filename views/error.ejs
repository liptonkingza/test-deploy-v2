<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Siamdrug App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header bg-danger text-white">
                        <h4 class="mb-0">เกิดข้อผิดพลาด</h4>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-danger">
                            <h5>Error Message:</h5>
                            <pre class="mb-0"><%= typeof error === 'string' ? error : (error && error.message ? error.message : 'No error message provided') %></pre>
                        </div>
                        
                        <div class="alert alert-info">
                            <h6>Error Type:</h6>
                            <pre><%= typeof error %></pre>
                        </div>
                        
                        <% if (typeof error === 'object' && error) { %>
                        <div class="alert alert-secondary">
                            <h6>Error Object Keys:</h6>
                            <pre><%= Object.keys(error).join(', ') %></pre>
                        </div>
                        <% } %>
                        
                        <% if (typeof error === 'object' && error && error.stack) { %>
                        <div class="alert alert-warning">
                            <h6>Stack Trace:</h6>
                            <pre style="font-size: 11px; max-height: 300px; overflow-y: auto;"><%= error.stack %></pre>
                        </div>
                        <% } %>
                        
                        <div class="alert alert-light">
                            <h6>Full Error Debug:</h6>
                            <pre style="font-size: 10px; max-height: 400px; overflow-y: auto;"><%= JSON.stringify(error, null, 2) %></pre>
                        </div>
                        <div class="mt-3">
                            <a href="/workflow" class="btn btn-primary me-2">กลับไปหน้า Workflow</a>
                            <a href="/dashboard" class="btn btn-secondary">ไปหน้า Dashboard</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
