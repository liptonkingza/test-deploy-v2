# วิธี Import ไฟล์ SQL ใน MySQL Workbench

## ⚠️ ปัญหาที่พบบ่อย

**Error**: "This file type is not supported, valid options are: CSV, JSON"

**สาเหตุ**: ใช้ "Table Data Import Wizard" ซึ่งรองรับเฉพาะ CSV และ JSON เท่านั้น

## ✅ วิธีที่ถูกต้องในการ Import ไฟล์ SQL

### วิธีที่ 1: ใช้ Server > Data Import (แนะนำ)

1. **เชื่อมต่อกับ Railway Database**
   - เปิด MySQL Workbench
   - เชื่อมต่อกับ Railway database

2. **เปิด Data Import**
   - ไปที่เมนู **Server** > **Data Import**
   - หรือกด **Ctrl+Shift+I**

3. **ตั้งค่าการ Import**
   - เลือก **Import from Self-Contained File**
   - คลิก **...** เพื่อเลือกไฟล์ SQL (`sql/users (1).sql`)
   - ในส่วน **Default Target Schema** เลือก `railway` (หรือชื่อ database ของคุณ)
   - ตรวจสอบว่า **Create New Schema** ไม่ถูกเลือก (ถ้ามี schema อยู่แล้ว)

4. **เริ่ม Import**
   - คลิก **Start Import** ที่มุมล่างขวา
   - รอจนเสร็จ (จะเห็น progress bar และ log)

### วิธีที่ 2: เปิดไฟล์ SQL แล้วรัน (ง่ายกว่า)

1. **เปิดไฟล์ SQL**
   - ไปที่เมนู **File** > **Open SQL Script**
   - หรือกด **Ctrl+O**
   - เลือกไฟล์ `sql/users (1).sql`

2. **เลือก Schema**
   - ดูที่แถบด้านบนของหน้าต่าง Query
   - คลิก dropdown ที่แสดง schema และเลือก `railway`

3. **รันสคริปต์**
   - กด **Ctrl+Shift+Enter** (Execute All)
   - หรือคลิกปุ่ม ⚡ (Execute) ที่แถบเครื่องมือ
   - รอจนเสร็จ

4. **ตรวจสอบผลลัพธ์**
   - ดูที่แท็บ **Output** หรือ **Action Output** ด้านล่าง
   - ควรเห็น "Query OK" หรือ "0 row(s) affected" สำหรับแต่ละคำสั่ง

### วิธีที่ 3: Copy-Paste และรัน

1. เปิดไฟล์ `sql/users (1).sql` ด้วย text editor (Notepad++, VS Code)
2. Copy ทั้งหมด (Ctrl+A, Ctrl+C)
3. ใน MySQL Workbench:
   - เปิดแท็บ Query ใหม่ (Ctrl+T)
   - ตรวจสอบว่าเลือก schema `railway` ไว้
   - Paste (Ctrl+V)
   - กด **Ctrl+Shift+Enter** เพื่อรัน

## 🔍 ตรวจสอบผลลัพธ์

หลังจาก import เสร็จแล้ว ให้รันคำสั่งนี้เพื่อตรวจสอบ:

```sql
-- ตรวจสอบจำนวนข้อมูล
SELECT COUNT(*) as total_users FROM `users`;

-- ตรวจสอบข้อมูลบางส่วน
SELECT id, operator, pn_id, source, level 
FROM `users` 
LIMIT 10;

-- ตรวจสอบว่ามีค่า NULL ในคอลัมน์ source (ควรมี)
SELECT COUNT(*) as null_source_count 
FROM `users` 
WHERE `source` IS NULL;
```

## ❌ สิ่งที่ห้ามทำ

- ❌ อย่าใช้ "Table Data Import Wizard" สำหรับไฟล์ SQL
- ❌ อย่าใช้ "Table Data Import" จาก context menu (คลิกขวาที่ตาราง)

## ✅ สิ่งที่ควรทำ

- ✅ ใช้ **Server > Data Import** สำหรับไฟล์ SQL
- ✅ หรือเปิดไฟล์ SQL แล้วรันด้วย Execute
- ✅ ตรวจสอบ schema ก่อนรันคำสั่ง

## 🆘 ถ้ายังมีปัญหา

1. **ตรวจสอบ Connection**
   - ตรวจสอบว่าเชื่อมต่อกับ Railway database ได้
   - ทดสอบด้วยคำสั่ง: `SELECT DATABASE();`

2. **ตรวจสอบ Schema**
   - ตรวจสอบว่ามี schema `railway` อยู่แล้ว
   - ถ้ายังไม่มี ให้สร้างก่อน: `CREATE DATABASE railway;`

3. **ตรวจสอบไฟล์ SQL**
   - เปิดไฟล์และตรวจสอบว่าไม่มี syntax error
   - ตรวจสอบว่าแก้ไข `'0000-00-00'` เป็น `NULL` แล้ว

4. **ดู Error Log**
   - ดูที่แท็บ **Action Output** หรือ **Output** ด้านล่าง
   - จะมี error message บอกว่าปัญหาอยู่ที่ไหน

